// Test stub: the real 'server-only' package throws when imported outside a
// React Server Component. Vitest aliases to this so unit tests can import
// server modules (e.g. lib/booking-export.ts) and test the REAL code.
export {};
